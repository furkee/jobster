{
  "name": "postgres",
  "version": "1.0.0",
  "main": "lib/index.ts",
  "type": "module",
  "scripts": {
    "dev": "node --watch --no-warnings=ExperimentalWarning --experimental-strip-types lib/main.ts",
    "start": "node build/main.js",
    "build": "rm -rf build || true && tsc"
  },
  "peerDependencies": {
    "jobster": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.7.4"
  }
}
